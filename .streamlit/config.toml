[server]
# 파일 변경 시 자동 리로드 비활성화 (코드 수정 중 파이프라인 중단 방지)
fileWatcherType = "none"

# 세션 타임아웃: 기본 300초 → 3600초 (1시간)
# 파이프라인이 오래 걸려도 세션 유지
enableStaticServing = false

[runner]
# 위젯 변경 시 자동 재실행 비활성화하지 않음 (기본 동작 유지)
# 하지만 파일 변경 리로드는 위에서 비활성화

[browser]
# 서버 연결 끊김 허용 시간
serverPort = 8501
